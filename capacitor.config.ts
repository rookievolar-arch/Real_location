import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reallocation.app',
  appName: 'Real_location',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
